import React from 'react';
import { PrivacyPolicy } from '@/components/PrivacyPolicy';

/** プライバシーポリシー画面（設定から開く）。本文は未ログインでも表示できる共通コンポーネント。 */
export default function PrivacyScreen() {
  return <PrivacyPolicy />;
}
